# Default model used by Codex in this repository
model = "gpt-5.4"

# Ask before running commands that modify the system
approval_policy = "on-request"

# Optional reasoning level
model_reasoning_effort = "extra_high"

# Treat directories containing .git as project root
project_root_markers = [".git"]